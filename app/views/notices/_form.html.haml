= simple_form_for @notice, html: { multipart: true } do |f|
  - if @notice.errors.any?
    #errors
      %p
        = @notice.errors.count
        Prevented this recipe from saving
      %ul
        = @notice.errors.full_messages.each do |msg|
          %li= msg
  .panel-body
    = f.label 'Título Noticia'
    = f.input :title, input_html: { class: 'form-control' }, label: false
    = f.input :subtitle, input_html: {class: 'form-control'}
    = f.label 'Cuerpo Noticia'
    = f.input :body, input_html: { class: 'form-control' }, label: false
    = f.input :image, input_html: { class: 'form-control' }
    = f.input :date, input_html: {class: 'form-control'}


  = f.button :submit, class: "btn btn-primary"